import { ExpoConfig } from 'expo/config';

export default (): ExpoConfig => ({
  name: 'Track Analyzer',
  slug: 'track-analyzer',
  extra: {
    // optional legacy usage
  },
});
export default {
  name: 'Track Analyzer',
  slug: 'track-analyzer',
  extra: {},
};
